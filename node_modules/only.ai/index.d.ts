import EventEmitter from "node:events"
import {sor, sorv2 } from "only ai"
declare module "only.ai"{
    export class onlyai {
        public sor(soru: string) : void;
        public sorv2(soru: string) : void;
    }
}